import { SCHOOLENDPOINTS } from "../endpoints";
import { getRequest } from "../requests";

export const getSchools = async () => {
  //   let respose = await getRequest(SCHOOLENDPOINTS.GET_SCHOOLS);
  await setTimeout(() => {}, 100);
  return { data: "" };
};
